<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s331153s333975mappe2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".AktivitetMoteDeltagelse"></activity>
        <activity android:name=".AktivitetMote">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AktivitetKontakt"></activity>
        <activity android:name=".Aktivitet_MoteInnhold"></activity>
        <activity android:name=".RegistrerMote"></activity>
    </application>

</manifest>